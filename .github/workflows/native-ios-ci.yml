name: Native iOS CI

on:
  pull_request:
    branches:
      - apple
    paths:
      - "native-ios/**"
      - ".swiftlint.yml"
      - ".github/workflows/native-ios-ci.yml"
  push:
    branches:
      - apple
    paths:
      - "native-ios/**"
      - ".swiftlint.yml"
      - ".github/workflows/native-ios-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: native-ios-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Check WhatsApp bridge JavaScript syntax
        run: |
          set -euo pipefail
          node --check native-ios/WhatsAppTranslator/WhatsAppBridge.js

      - name: Run SwiftLint on Ubuntu
        run: |
          set -euo pipefail
          docker run --rm \
            -v "$GITHUB_WORKSPACE:/workspace" \
            -w /workspace \
            ghcr.io/realm/swiftlint:0.65.1 \
            lint --strict --config .swiftlint.yml

  test-core:
    name: test-core
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run host-independent Swift tests on Linux
        run: |
          set -euo pipefail
          docker run --rm \
            -v "$GITHUB_WORKSPACE:/workspace" \
            -w /workspace \
            swift:6.0-noble \
            bash -lc 'apt-get update >/dev/null && \
              apt-get install -y --no-install-recommends libsqlite3-dev >/dev/null && \
              swift test \
                --package-path native-ios \
                --configuration debug \
                --parallel'

  build-apple:
    name: build-apple
    runs-on: xcode-27
    needs:
      - lint
      - test-core
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Show Apple toolchain
        run: |
          set -euo pipefail
          sw_vers
          xcodebuild -version
          xcodebuild -showsdks

      - name: Inspect project
        run: |
          set -euo pipefail
          xcodebuild \
            -project native-ios/WhatsAppTranslator.xcodeproj \
            -list

      - name: Build native iOS target for Simulator SDK
        run: |
          set -euo pipefail
          xcodebuild \
            -project native-ios/WhatsAppTranslator.xcodeproj \
            -target WhatsAppTranslator \
            -configuration Debug \
            -sdk iphonesimulator \
            CODE_SIGNING_ALLOWED=NO \
            clean build

      - name: Build temporary Qwen MLX harness
        working-directory: native-ios/MLXHarness
        run: |
          set -euo pipefail
          xcodebuild \
            -scheme QwenMLXHarness \
            -configuration Release \
            -destination 'platform=macOS,arch=arm64' \
            -derivedDataPath "$PWD/.xcodebuild" \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            build

      - name: Smoke-test Qwen MLX inference
        working-directory: native-ios/MLXHarness
        run: |
          set -euo pipefail
          ./.xcodebuild/Build/Products/Release/QwenMLXHarness \
            --prompt "Reply only with OK." \
            --max-tokens 16 \
            --temperature 0

      - name: Record validation boundaries
        run: |
          cat <<'EOF'
          Ubuntu CI validates Swift style, WhatsAppBridge.js syntax, and all current
          host-independent deterministic Swift tests, including bridge/session contracts,
          WhatsAppWebTransport request/response/event orchestration, translation/context logic,
          and the SQLite domain persistence layer. The Xcode runner compiles the Apple-specific
          WebKit runtime, SQLite persistence source, and application against the iOS Simulator SDK.

          The temporary MLX validation builds the isolated macOS QwenMLXHarness with Xcode so
          MLX Metal resources are bundled, then downloads mlx-community/Qwen3-0.6B-4bit and runs
          a short deterministic generation. It does not add MLX to the production app target.

          Neither Linux tests nor the Simulator build validate Foundation Models inference,
          Apple Intelligence availability, real WhatsApp Web authentication, the current
          WhatsApp-private runtime adapter, linked-device persistence, force-quit restoration,
          offline resync, or a real group message arriving through WebKit on a physical iPhone.
          Those acceptance gates remain open in their dedicated roadmap issues.
          EOF
