# P0.1 Apple SystemLanguageModel benchmark

Status: physical-device benchmark completed on 2026-09-08; Apple SystemLanguageModel rejected for the contextual Indonesian -> Polish product direction.

Parent issue: #3
Implementation slice: #38
Runtime/result follow-up: #39

## Purpose

This benchmark decides whether Apple's on-device `SystemLanguageModel` is viable for contextual Indonesian -> Polish WhatsApp translation.

The benchmark intentionally separates three failure modes:

1. **Model availability failure** — the system model is unavailable on the test device or region/account state.
2. **Unsupported-language/runtime failure** — the API rejects Indonesian, Polish, mixed-language, or contextual prompts.
3. **Poor translation quality** — the route runs but the output is not good enough for the product.

## How to run

1. Open `native-ios/WhatsAppTranslator.xcodeproj` in Xcode.
2. Select the `WhatsAppTranslator` target.
3. Select a physical Apple-Intelligence-capable iPhone.
4. Build and run the app.
5. Open **P0.1 SystemLanguageModel Benchmark** from the diagnostics screen.
6. Tap **Run P0.1 benchmark**.
7. Tap **Copy all results**, then paste the Markdown export from the clipboard into this document.
8. Review output quality manually and choose a decision below.

Do not close P0.1 from simulator-only results.

## Benchmark matrix

The app runs these source-controlled synthetic cases:

| Case | Route | Context messages | Main risk |
| --- | --- | ---: | --- |
| ID -> EN particles and slang | Indonesian -> English | 0 | `dia`, `dong`, `sih`, `nih`, `mager`, `wkwk` |
| EN -> PL natural chat | English -> Polish | 0 | Natural informal Polish tone |
| ID -> PL omitted subject | Indonesian -> Polish | 0 | `kok`, `masa`, `sih`, omitted subject, `nggak/gak` variants |
| English prompt: ID -> PL | Indonesian -> Polish (English prompt) | 0 | Whether English-only framing bypasses language gating |
| ASCII/Base64 prompt: ID -> PL | Indonesian -> Polish (ASCII/Base64 envelope) | 0 | Whether an ASCII-only encoded envelope bypasses language gating |
| ASCII/Unicode-escape prompt: ID -> PL | Indonesian -> Polish (ASCII/Unicode-escape envelope) | 0 | Whether JSON Unicode escapes bypass language gating and remain decodable |
| ASCII/Unicode-escape body prompt: ID -> PL | Indonesian -> Polish (ASCII/Unicode-escape body envelope) | 0 | Smaller escape payload that avoids full-JSON token blow-up |
| Contextual ID -> PL (0) | Contextual Indonesian -> Polish | 0 | Baseline without prior context |
| Contextual ID -> PL (3) | Contextual Indonesian -> Polish | 3 | Short context sensitivity |
| Contextual ID -> PL (8) | Contextual Indonesian -> Polish | 8 | Medium context sensitivity |
| Contextual ID -> PL (16) | Contextual Indonesian -> Polish | 16 | Larger context sensitivity |

## Physical-device evidence and provenance

- Device: physical iPhone 15 Pro Max (`iPhone16,2`), verified through CoreDevice before installation.
- Source revision: `0c10a58c98bc3d67b9b74dcba47faa3336321890` (`apple` at build time).
- Build: Debug, Xcode 26.6 (17F113), iPhoneOS 26.5 SDK; signed build, installation, and launch succeeded on the physical device.
- iOS: 26.6.1 (23G83), confirmed by both CoreDevice and the export.
- Evidence: user supplied the in-app Markdown export after launching this build; the run timestamp is 2026-09-08T18:48:04Z.
- Apple Intelligence setting: not separately recorded; the runtime reports the model available.
- Region and device language settings: not recorded.
- Model version: not exposed in this export.
- Scope: one run of synthetic fixtures. Latencies are individual harness measurements, not cold/warm distributions or throughput measurements. No physical-device Apple Translation framework result was supplied.

## Raw physical-device export

The run metadata and results below are preserved verbatim from the supplied export. Its unfilled review and decision template is replaced by the assessment later in this report. `success` means the API returned text, not that translation quality passed.

# P0.1 Apple SystemLanguageModel benchmark results

## Run metadata

- Timestamp: 2026-09-08T18:48:04Z
- OS: Version 26.6.1 (Build 23G83)
- Bundle: com.mmatlacz.WhatsAppTranslator
- Model availability: available
- Context size: 4096 tokens
- Supported languages count: 23
- Supports Indonesian id_ID: no / not advertised
- Supports Polish pl_PL: no / not advertised
- Translation prompt versions: base64-encoded-translation-v1, contextual-translation-v1, english-prompt-translation-v1, unicode-escaped-body-translation-v1, unicode-escaped-translation-v1

## Results

| Case | Route | Context messages | Latency | Result | Error category | Output / error |
| --- | --- | ---: | ---: | --- | --- | --- |
| ID -> EN particles and slang | Indonesian -> English | 0 | 33 ms | error | unsupported-language/runtime | unsupportedLanguageOrLocale(FoundationModels.LanguageModelSession.GenerationError.Context(debugDescription: "Unsupported language.", underlyingErrors: [], errorDescriptionOverride: nil)) |
| EN -> PL natural chat | English -> Polish | 0 | 2197 ms | success | n/a | Powiedziała, że może później. Jest naprawdę zbyt zmęczona, żeby wyjść teraz, więc nie zmuszaj ją, haha. |
| ID -> PL omitted subject | Indonesian -> Polish | 0 | 8 ms | error | unsupported-language/runtime | unsupportedLanguageOrLocale(FoundationModels.LanguageModelSession.GenerationError.Context(debugDescription: "Unsupported language.", underlyingErrors: [], errorDescriptionOverride: nil)) |
| Contextual ID -> PL (0) | Contextual Indonesian -> Polish | 0 | 5 ms | error | unsupported-language/runtime | unsupportedLanguageOrLocale(FoundationModels.LanguageModelSession.GenerationError.Context(debugDescription: "Unsupported language.", underlyingErrors: [], errorDescriptionOverride: nil)) |
| Contextual ID -> PL (3) | Contextual Indonesian -> Polish | 3 | 4 ms | error | unsupported-language/runtime | unsupportedLanguageOrLocale(FoundationModels.LanguageModelSession.GenerationError.Context(debugDescription: "Unsupported language.", underlyingErrors: [], errorDescriptionOverride: nil)) |
| Contextual ID -> PL (8) | Contextual Indonesian -> Polish | 8 | 4 ms | error | unsupported-language/runtime | unsupportedLanguageOrLocale(FoundationModels.LanguageModelSession.GenerationError.Context(debugDescription: "Unsupported language.", underlyingErrors: [], errorDescriptionOverride: nil)) |
| Contextual ID -> PL (16) | Contextual Indonesian -> Polish | 16 | 7 ms | error | unsupported-language/runtime | unsupportedLanguageOrLocale(FoundationModels.LanguageModelSession.GenerationError.Context(debugDescription: "Unsupported language.", underlyingErrors: [], errorDescriptionOverride: nil)) |
| English prompt: ID -> PL | Indonesian -> Polish (English prompt) | 0 | 6 ms | error | unsupported-language/runtime | unsupportedLanguageOrLocale(FoundationModels.LanguageModelSession.GenerationError.Context(debugDescription: "Unsupported language.", underlyingErrors: [], errorDescriptionOverride: nil)) |
| ASCII/Base64 prompt: ID -> PL | Indonesian -> Polish (ASCII/Base64 envelope) | 0 | 6740 ms | success | n/a | ```json<br>{<br>  "recentTurns": [<br>    {<br>      "turn": 1,<br>      "text": "Hi there! I'm just a chat assistant, so I don't have any personal information about you. What can I help you with today?"<br>    },<br>    {<br>      "turn": 2,<br>      "text": "Hello! I'm a student and I'm currently studying for my exams. What topics are you interested in?"<br>    },<br>    {<br>      "turn": 3,<br>      "text": "Hi there! I'm a teacher and I'm currently teaching a class on programming. What programming languages are you interested in learning?"<br>    }<br>  ],<br>  "quotedTurn": [<br>    {<br>      "turn": 1,<br>      "text": "Hello! I'm just a chat assistant, so I don't have any personal information about you. What can I help you with today?"<br>    },<br>    {<br>      "turn": 2,<br>      "text": "Hello! I'm a student and I'm currently studying for my exams. What topics are you interested in?"<br>    },<br>    {<br>      "turn": 3,<br>      "text": "Hi there! I'm a teacher and I'm currently teaching a class on programming. What programming languages are you interested in learning?"<br>    }<br>  ],<br>  "summary": "This is a conversation between a chat assistant and a student. The chat assistant provides information about its capabilities and the student asks about topics they are interested in studying. The chat assistant then responds with information about programming languages that are currently being taught in its class."<br>}<br>```<br><br>```json<br>{<br>  "target.body": "Hi there! I'm just a chat assistant, so I don't have any personal information about you. What can I help you with today?"<br>}<br>``` |
| ASCII/\u prompt: ID -> PL | Indonesian -> Polish (ASCII/\u envelope) | 0 | 2587 ms | success | n/a | ```<br>{"body": "I'm so sorry for being late. I had a flat tire and it took me a while to get here. I'll make sure to drive more carefully next time."}<br>``` |
| ASCII/\u body prompt: ID -> PL | Indonesian -> Polish (ASCII/\u body envelope) | 0 | 949 ms | success | n/a | My friend, I'm sorry, but I cannot help you with that. |

## Simulator control result (not a physical-device decision)

Observed on the iPhone 17 Pro simulator, iOS 26.5 (Build 23F77), with the
model available, a 4096-token context, and 23 advertised languages. Neither
`id_ID` nor `pl_PL` was advertised.

- Raw Indonesian -> English, raw Indonesian -> Polish, all contextual
  Indonesian -> Polish cases, and the English-only prompt control failed with
  `unsupportedLanguageOrLocale` / `Unsupported language.`
- English -> Polish succeeded with a natural Polish translation.
- The ASCII/Base64 envelope avoided the immediate language error, but the model
  returned unrelated fabricated JSON instead of decoding the payload.
- The full ASCII/Unicode-escape envelope hit `exceededContextWindowSize` at
  4,091 tokens out of the 4,096-token limit on the latest run.
- The smaller body-only ASCII/Unicode-escape experiment avoided the context
  overflow, but returned only an empty code-fence fragment ("``` ``` ```"),
  not a Polish translation.
- The separate iOS `Translation` framework probe reported the `id -> pl` pair
  as `unsupported` on the same simulator, so it did not provide a local
  fallback either.

These routes are therefore diagnostic experiments only; they are not a general
Indonesian fallback and must not be enabled in the product translator.

## Manual quality notes — physical iPhone

This is a review of the supplied output, not a separate native-speaker study. Runtime rejections are not scored as poor translations because they produced no translation.

| Case | Assessment |
| --- | --- |
| Indonesian -> English | Unsupported-language/runtime rejection in 33 ms; no output to assess. |
| English -> Polish | Polish chat text returned in 2,197 ms. The phrase “nie zmuszaj ją” is grammatically incorrect; “nie zmuszaj jej” is the expected negated form. One output does not establish route reliability, and Polish is not advertised as supported. |
| Indonesian -> Polish | Unsupported-language/runtime rejection in 8 ms; no output to assess. |
| English prompt: Indonesian -> Polish | Unsupported-language/runtime rejection in 6 ms; English framing did not make this route usable. |
| ASCII/Base64 prompt: Indonesian -> Polish | Returned fabricated English conversation JSON in 6,740 ms, not a Polish translation. API completion, translation failure. |
| ASCII/Unicode-escape prompt: Indonesian -> Polish | Returned an English flat-tire apology in 2,587 ms, not a Polish translation. API completion, translation failure. Unlike the simulator control, this run did not report context overflow. |
| ASCII/Unicode-escape body prompt: Indonesian -> Polish | Returned an English refusal in 949 ms, not a translation. API completion, translation failure. |
| Contextual Indonesian -> Polish, 0 messages | Unsupported-language/runtime rejection in 5 ms; context quality cannot be assessed. |
| Contextual Indonesian -> Polish, 3 messages | Unsupported-language/runtime rejection in 4 ms; context quality cannot be assessed. |
| Contextual Indonesian -> Polish, 8 messages | Unsupported-language/runtime rejection in 4 ms; context quality cannot be assessed. |
| Contextual Indonesian -> Polish, 16 messages | Unsupported-language/runtime rejection in 7 ms; context quality cannot be assessed. |

## Decision

- [ ] Apple SystemLanguageModel is viable as the primary translator.
- [ ] Apple SystemLanguageModel is partially viable and needs fallback.
- [x] Apple SystemLanguageModel is rejected for this product direction.

The model was available, so this is not an availability failure. All seven unencoded Indonesian cases (including English framing and all four context sizes) were rejected with unsupported-language/runtime errors. All three encoded variants completed without an API error but failed to produce a Polish translation. Thus none of the ten Indonesian-input cases established a usable translation route on this physical iPhone.

The single English -> Polish completion does not satisfy the core contextual Indonesian -> Polish requirement and does not justify selecting this model as a partially viable product translator. The rejection applies to the tested product direction and this device/software run; it is not a claim that every language route or future model version is unusable. Short rejection latencies measure failure, not translation performance.

Stop prompt-envelope experiments for this decision. This evidence justifies planning the next measured local-model slice under #4; it does not select Qwen or enable a production fallback. Split implementation and physical-device validation into focused issues before coding. Require comparable P0.1 fixtures, completed translations, quality review, and device performance measurements before choosing a default/fallback model.

## Closure rule

This report satisfies the runtime/results scope of #39. Its results PR should close **#39 only**. Parent #3 must be assessed and finalized separately against all of its acceptance criteria. The simulator control remains historical evidence, distinct from this physical-device run.

WebKit feasibility (#5) and pairing/persistence (#6) require their own physical-device evidence and separate validation PRs. This benchmark establishes neither transport gate.
